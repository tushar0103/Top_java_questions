class Solution {
    public boolean isPalindrome(String s) {
       s.toLowerCase();
       int l=s.length();
        for(int i=0; i<l; i++){
            if(s.charAt(i)==s.charAt(l-i-1))
                return true;
            
        }
        return false;
        
    }   
}

